# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Test / coverage
coverage/

# Secrets & environment — keep .env.example, ignore real env files
.env
.env.*
!.env.example
*.local

# Databases (never commit Garmin data or the app's own state)
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3

# Deploy secrets — keep the .example, ignore the real single-account allowlist
deploy/authenticated-emails.txt

# Logs
*.log
npm-debug.log*

# OS / editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/
